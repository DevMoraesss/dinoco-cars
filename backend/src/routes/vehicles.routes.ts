import { Router } from 'express';
import VehicleController from '../controllers/vehicles.controller'; // Import atualizado

const vehicleRouter = Router();

// Endpoint atualizado para /vehicles


export default vehicleRouter;